You are working with the LiveC codebase and the attached "Prototype Requirements" document (SRS). Your task is to analyze the current codebase and generate a structured report based on the SRS.

Specifically, do the following:

1. **Project Purpose**  
Summarize what this software is designed to do, as reflected in both the SRS and current implementation.

2. **Implemented Features**  
List which features from the SRS are clearly implemented in the codebase. Use file paths and function/component names to justify your assessment.

3. **Incomplete or Missing Features**  
List SRS features or user stories that are partially implemented, stubbed, or missing entirely from the codebase.

4. **Observations & Discrepancies**  
Note any mismatches between the SRS and the implementation (e.g. architectural changes, scope reductions, or UI deviations).

Use these reference points from the SRS:
- Section 5: Roles (Community Member, AE, Reviewer, EIC, etc.)
- Section 6: System Features and user stories (e.g., CM1–CM5, AE1–AE9, R1–R3)
- Section 10: Process diagrams (DFDs and flowcharts)

Be specific when referencing files and explain how each feature maps to the system's behavior. Group findings by user role where possible.
